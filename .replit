# Replit configuration for Flupp backend
run = "cd backend/flupp && npm start"
modules = ["nodejs-20"]

[nix]
channel = "stable-22_11"

[deployment]
run = ["sh", "-c", "cd backend/flupp && npm start"]

[env]
PORT = "8787"
HOST = "0.0.0.0"
NODE_ENV = "production"

[[ports]]
localPort = 8787
externalPort = 80